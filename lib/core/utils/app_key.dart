class AppKey {
  // Key for storing and retrieving the user ID
  static const userId = 'userId';

  // Key for storing and retrieving stories (currently using the same key as userId)
  static const stories = 'userId';

  // Key for storing and retrieving comments (currently using the same key as userId)
  static const comments = 'userId';

  // Key for storing and retrieving posts (currently using the same key as userId)
  static const posts = 'userId';
}
